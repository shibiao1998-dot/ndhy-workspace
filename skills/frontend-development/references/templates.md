# 前端实现交付模板

## 1. 实现说明文档 (`implementation-notes.md`)

```markdown
# {项目名} 前端实现说明

## 概述
- **实现范围**: {页面/模块清单}
- **技术栈**: {框架/UI库/状态管理/请求库}
- **设计稿**: {链接}
- **API契约**: {链接}

## 页面清单

| 页面 | 路由 | 状态 | 说明 |
|------|------|------|------|
| {页面名} | `/path` | ✅ 完成 | {简要说明} |

## 组件树概览
{组件层次结构，标注容器/展示/Hook}

## 状态管理方案
- **服务端状态**: {React Query / SWR / RSC}
- **全局状态**: {Context / Zustand / 无}
- **表单状态**: {React Hook Form / 受控组件}
- **URL 状态**: {SearchParams / 无}

## 关键决策
| 决策 | 选择 | 理由 |
|------|------|------|
| {决策项} | {选择} | {理由} |
```

## 2. 组件树文档 (`component-tree.md`)

```markdown
# 组件树与 Props 接口

## 组件层次

{PageName}/
├── {PageName}Page           # 页面容器（数据获取、路由参数）
│   ├── {SectionA}           # 区域组件
│   │   ├── {ComponentX}     # 展示组件
│   │   └── {ComponentY}     # 展示组件
│   └── {SectionB}
│       └── {ComponentZ}
└── hooks/
    ├── use{Feature}         # 业务逻辑 Hook
    └── use{DataSource}      # 数据获取 Hook

## Props 接口定义

### {ComponentName}

| Prop | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| {propName} | `{type}` | ✅/❌ | {default} | {说明} |

### 事件回调

| 回调 | 参数 | 触发时机 |
|------|------|---------|
| on{Event} | `{params}` | {说明} |
```

## 3. API 接入说明 (`api-integration.md`)

```markdown
# API 接入说明

## 接口调用清单

| 接口 | 方法 | 用途 | 调用方 | Mock 状态 |
|------|------|------|--------|-----------|
| `{endpoint}` | GET/POST | {用途} | {组件/Hook名} | ✅/❌ |

## 类型定义
{列出关键 Request/Response 的 TypeScript 类型}

## 错误码映射

| 错误码 | 含义 | UI 反馈 |
|--------|------|---------|
| {code} | {含义} | {用户看到什么} |

## 认证方式
{Token/Cookie/Session 说明}
```

## 4. 已知限制文档 (`known-limitations.md`)

```markdown
# 已知限制与待确认项

## 已知限制

| # | 限制 | 影响范围 | 规避方案 | 优先级 |
|---|------|---------|---------|--------|
| 1 | {限制描述} | {影响} | {规避} | P0-P3 |

## 待确认项

| # | 问题 | 需要确认方 | 状态 |
|---|------|-----------|------|
| 1 | {问题描述} | {API/体验/架构} | 待确认 |

## 技术债务

| # | 债务 | 原因 | 建议处理时间 |
|---|------|------|-------------|
| 1 | {描述} | {为什么产生} | {建议时间} |
```

## 5. 联调交接清单 (`handoff-checklist.md`)

```markdown
# 联调交接清单

## 前置条件

- [ ] API 服务已部署到 {环境}
- [ ] 测试账号已准备: {账号信息}
- [ ] Mock 数据已关闭 / 已切换到真实接口
- [ ] 环境变量已配置: {列表}

## 接口联调状态

| 接口 | 前端状态 | 后端状态 | 联调结果 | 备注 |
|------|---------|---------|---------|------|
| `{endpoint}` | ✅ 已对接 | ⬜ 待确认 | ⬜ 待联调 | {备注} |

## 联调步骤

1. {步骤描述}
2. {步骤描述}

## 联调注意事项
- {注意事项}

## 问题记录

| # | 问题 | 发现时间 | 责任方 | 状态 |
|---|------|---------|--------|------|
```

## 6. 测试覆盖说明 (`test-coverage.md`)

```markdown
# 测试覆盖说明

## 测试概览

| 类型 | 数量 | 通过 | 失败 | 跳过 |
|------|------|------|------|------|
| 组件测试 | {n} | {n} | {n} | {n} |
| 交互测试 | {n} | {n} | {n} | {n} |
| 契约校验 | {n} | {n} | {n} | {n} |

## 组件测试清单

| 组件 | 测试文件 | 覆盖场景 |
|------|---------|---------|
| {ComponentName} | `{path}` | 渲染/Props/状态/事件 |

## 交互测试清单

| 用户路径 | 测试文件 | 覆盖步骤 |
|---------|---------|---------|
| {路径描述} | `{path}` | {步骤概要} |

## 未覆盖区域
- {描述 + 原因 + 风险评估}
```
