# 2026-03-18 工作日志

## AIAE 网关多模型路由 Skill 创建

### 任务来源
老板要求（04:03 飞书DM）：深度阅读 AIAE-GATEWAY-API-GUIDE.md，调研每个模型能力，设计专家×任务×模型匹配策略，创建 Skill 落地。

### 完成内容
1. **API 连通性全面验证**：
   - GET /v1/models ✅（138 个模型确认可用）
   - Doubao Seed 2.0 Pro 文本对话 ✅（¥0.005/次）
   - DeepSeek V3.2 文本对话 ✅（¥0.0001/次，极低成本）
   - Gemini 3.1 Pro Preview 文本对话 ✅
   - Gemini 3.1 Flash Image 图像生成 ✅（~3MB base64 返回）
   - GPT Image 1 图像生成 ✅（~1.6MB base64 返回）
   - PowerShell JSON 转义坑已解决（写文件+@引用）

2. **模型能力深度调研**（8 轮 web_search）：
   - Claude Opus 4.6 vs GPT-5.4：Claude 编程 #1（SWE-Bench 81.4%），GPT 通用 #1
   - Nano Banana 2：Image Arena 全球 #1，4K，文字渲染极佳
   - Doubao Seed 2.0 Pro：SuperCLUE 国内第一，AIME25 98.3
   - Qwen 3.5 Plus：速度 Sonnet 1/6，IFBench 76.5
   - O3/O4-Mini：推理专精，o4-mini 性价比最佳
   - Sora 2 vs Kling：Sora 运镜+音频最强，Kling 4K
   - GPT Image 1.5：指令遵循之王
   - Kimi K2：1T 参数 MoE，Agent 能力强

3. **model-router Skill 创建完成**：
   ```
   skills/model-router/
   ├── SKILL.md (5.5KB) — 快速决策树 + 核心路由表 + 专家匹配速查
   ├── references/
   │   ├── model-capabilities.md (3.2KB) — 全模型能力详解
   │   ├── image-models.md (4.6KB) — 图像模型对比+场景选型+调用方式
   │   ├── media-models.md (3.4KB) — 视频/语音/3D/嵌入
   │   └── api-examples.md (4.2KB) — 完整调用示例
   └── scripts/
       └── test_model.py (4.4KB) — 模型快速测试工具
   ```

4. **TOOLS.md 更新**：新增 AIAE 网关配置段落
5. **MEMORY.md 更新**：新增模型选用知识

### 关键发现
- Claude Opus 4.6 仍是 ACP 开发主力（编程 #1），但中文任务可切 Doubao 省钱提质
- 图像生成 Nano Banana 2 是当前 SOTA，UI 设计专家应默认使用
- 推理任务 o4-mini 性价比远超 o3，日常推理应优先
- 中文批量任务 Qwen 3.5 Plus 速度极快（1/6 Sonnet），适合高吞吐
- API 返回包含 cost_details（CNY），可做精确成本追踪

## 06:06 周度调研速报处理

### 三大行业趋势
1. **NVIDIA GTC 2026 NemoClaw**：集成 OpenClaw 的企业级 AI Agent 平台，17 家合作伙伴，Agent 进入企业部署阶段
2. **Harness Engineering 论文**：OpenAI 团队 100 万行代码 + 1500 PR，瓶颈从"写代码"转向"设计控制系统"
3. **Context Engineering 正式取代 Prompt Engineering**：行业共识 2026 是 Context Engineering 年

### 行动项（待老板确认）
- [ ] **claude-mem 集成评估**（37K⭐）：Claude Code 持久记忆插件，原生支持 OpenClaw。可能增强记忆管理
- [ ] **功能正确性验证环节**：当前流程偏重内部质量（lint/doc），需增加业务场景端到端验证
- [ ] **OpenViking 架构研究**：字节出品 L0/L1/L2 三级加载，比我们记忆系统更系统化

## 09:09 周度调研速报 #2 处理

### 新发现
- **Claude Code Agent Teams 实战案例**：5 Agent 并行游戏工作室，53% 任务完成。关键：`blockedBy` 依赖管理 + Agent "禁区"规则消除文件冲突 + 并行速度约串行 1/5
- **GSD-2（1.7K⭐）**：基于 Pi SDK 的 TypeScript CLI，实现 Token/cost 账本+仪表盘、卡死检测+三级超时恢复、Git worktree 隔离。是我们 ACP 监控能力的参考实现
- **Context Engineering 技能库（13.9K⭐）**：系统化 Agent 上下文工程最佳实践

### 新增行动项
- [ ] **子 Agent 指令包增加"禁止操作"清单**：学习 Agent Teams 的文件禁区规则，减少冲突
- [ ] **评估 GSD-2 的 token/cost 追踪**：补齐 ACP 监控短板
- [ ] **blockedBy 依赖管理机制**：Agent Teams 的自动阻塞未就绪任务，可优化指令包 L2 上游依赖

## 组织进化重大变更

### 需求处理门控流程 V2（老板批准，宪法级）
- **问题诊断**：老板花 80% 时间纠偏 Leader，根因是流程靠自觉执行，自觉不可靠
- **解法**：三级分流（⚡快速/🔧标准/🏗️复杂）+ 6 道门控（G0-G5），每个门控必须产出可见制品
- **老板角色变化**：从"全程监督"变为"4 个点审批"（确认分级→确认需求→批准方案→验收交付）
- **设计依据**：Superpowers 框架（强制管线）+ Context Engineering（上下文完整性）+ Agent 协作调研（对抗式协作）
- **已写入**：AGENTS.md 宪法 + MEMORY.md + 详细方案 `projects/org-evolution/request-processing-v2.md`

### ACP 实时监控机制（验证通过，宪法级）
- **问题**：ACP 是黑盒——spawn 后不知道进度、不知道完成、不知道卡死
- **解法三件套**：
  1. `streamTo: "parent"` — spawn 时加，返回 JSONL 日志可实时 tail
  2. `sessions_history` — 可读 ACP 会话完整历史
  3. `sessions_send` — 可向 ACP 追问/纠偏
- **配置变更**：
  - `tools.sessions.visibility`: "tree" → "all"
  - `tools.agentToAgent.enabled`: false → true
  - 两项均为热加载配置，不需要重启 Gateway
- **验证测试**：spawn ACP 任务 + stream 日志读取 + lifecycle 事件追踪，全部通过
- **已写入**：AGENTS.md 宪法 + MEMORY.md + TOOLS.md

### 调研报告全面复盘
- 读取并复盘了 10+ 份核心调研报告
- 提炼出组织痛点的三层解法：强制门控 + Agent 互查 + 上下文工程化
- 调研核心价值已吸收，报告可归档

## 工具使用教训
- **Gateway 重启用脱离进程的后台脚本**：老板纠正，`openclaw gateway restart` 会随 stop 断掉
- **部分配置支持热加载**：`agentToAgent` 等 dynamic reads 改完自动生效，不要白重启

## ACP 连通性测试
- 测试 1（00:00）：ACP spawn → 文件创建成功 → 任务完成 ✅
- 测试 2（00:47）：ACP spawn + streamTo → stream 日志完整追踪 3 个 Phase → lifecycle:end ✅
## 重要教训（V6 第二轮修复失败）

### ACP 连续修改必须有中间 commit
- **问题**：第一轮 ACP 完成 S3-S7 后直接启动第二轮修复，没有 commit
- **后果**：第二轮导致页面质量下降，回滚时无法只回滚第二轮——git stash 把两轮改动一起回滚
- **规则（宪法级）**：每轮 ACP 修改完成后必须先 git commit 再做下一轮。没有 commit = 没有存档点 = 无法安全回滚
- **当前状态**：两轮修改在 git stash（backup-round2-before-revert）中，页面回到 3/17 EOD 备份

### ACP 浏览器验证循环陷阱
- ACP 在 GSAP pin zone 中反复尝试滚动导致无限循环
- 教训：ACP 浏览器自动化不适合测试 GSAP pin/scrub 页面，改用 DOM 验证 + build 检查即可

### 三文档门控 + 模块化串行开发写入宪法（14:40）
- 老板要求新增强制流程节点，已写入 AGENTS.md 宪法和 MEMORY.md
- 新流程：Gate1 需求文档→老板确认 → Gate2 产品文档→老板确认 → Gate3 架构文档→老板确认 → 按架构拆模块→逐模块开发+验收→整体验收
- 三个文档未经老板确认不得进入下一阶段
- 一次只开发一个模块，模块验收通过后才能开发下一个

## V7 官网三文档门控执行（14:43 - 15:22）

### Gate 2 产品定义文档（14:43 - 14:55）
- 📐 产品定义专家 spawn，产出 `projects/ai-org-showcase/product-definition-v7.md`
- **产品概念**：一个多页静态网站，把 AI 组织的实时运行数据自动聚合展示
- **4 个场景 × 4 个页面**：S1 Landing(/) → S2 Team(/team) → S3 Capabilities(/capabilities) → S4 Activity(/activity)
- **5 个核心功能**：F1 Hero Hook · F2 组织全景页 · F3 能力矩阵页 · F4 活动流页 · F5 多页导航
- **老板决策**：
  - 整体方向 OK ✅
  - 成本透明（F6）MVP 不上，放 V2 ✅
  - 移动适配（F8）MVP 不上，放 V2 ✅
  - 指出专家数量不一致（27 vs 31），已修正为 30 专家（不含 Leader）
- **Gate 2 通过**：15:07 老板确认

### Gate 3 架构设计文档（15:08 - 15:22）
- 🏛️ 技术架构专家 spawn，产出 `projects/ai-org-showcase/architecture-v7.md`
- **架构模式**：SSG（静态站点生成）+ 离线数据管道，5 维评估 4.85/5
- **5 层系统架构**：数据源 → Python 采集（7 个采集器）→ data.json（单文件 50-100KB）→ Astro+React Islands → GitHub Pages
- **data.json Schema**：完整定义覆盖 4 页（org_info/experts+org_layers/skills+models/activity+git），含 `_meta` 版本化
- **8 个开发模块**：M1 数据管道 → M2 项目骨架 → M3 Landing → M4 组织全景 → M5 能力矩阵 → M6 活动流 → M7 CI/CD → M8 整体验收
- **性能目标**：LCP ≤1.5s · JS ≤150KB · Lighthouse ≥90
- **Gate 3 通过**：15:22 老板确认

### 老板指示并行开发策略（15:22）
- 老板明确要求：**UX/UI 设计和 M1 数据管道可以并行推进**
- 理由："还没到前端页面开发，先把后端做了"
- 要求：先让体验设计专家（UX）和 UI 设计专家分别产出交互文档和 MVP 原型
- 老板要求"先思考一下"再执行
- **我的分析**：
  - Track A：A1 UX 交互文档（体验设计专家）→ A2 UI 原型（UI 设计专家）【串行】
  - Track B：M1 数据管道开发（ACP）
  - A1 和 B 并行启动，A2 等 A1 完成后启动
  - UX 和 UI 之间是串行关系（UI 的输入来自 UX），不能同时做
  - 并行不违反模块化串行原则，因为 M1 是后端，设计是开发前置
- **待老板确认后执行**

## Gateway OOM 事件排查（14:55 - 15:04）

### 问题
- 老板发现 Gateway 断连，日志显示 `DataCloneError: Data cannot be cloned, out of memory`
- `Subagent announce failed` + `health post-connect health refresh failed`

### 根因
- **270 个 node.exe 僵尸进程，占用 15.1 GB 内存**
- 每个 ACP Claude Code 会话启动 ~8 个子进程（1 acp-wrapper + 1 claude-sdk + 4 MCP + 2 watchdog）
- 会话结束后子进程未被清理，持续堆积
- 当天跑了大量 ACP 任务导致内存耗尽

### 修复
- 清理 264 个僵尸进程（保留 6 个基础设施进程）
- 内存从 15.1 GB 降到 ~1 GB
- Gateway 重启

### 教训
- **ACP 会话进程泄漏是系统性问题**：需要加清理机制
- 每个 ACP 会话带 ~8 个 node 子进程，大量 ACP 任务后必须手动清理
- Gateway 进程自身也会累积状态（642 MB），需定期重启

## 调研速报 #2 关键发现（15:08）

### 粉色大象效应（Pink Elephant Problem）
- Gloaguen et al. 2026 研究：138 个代码库，LLM 生成的上下文文件降低成功率 20%+
- 告诉 Agent "不要用 X" 反而让 X 在注意力中被激活
- 我们的"禁止操作清单"应改为白名单式（"只操作这些"）
- 待 V7 完成后安排审计 AGENTS.md/CLAUDE.md 冗余约束

### Agent Teams vs Subagent 成本决策（行业共识）
- Subagent：4-7x token 消耗
- Agent Teams：~15x token 消耗
- 需建立成本决策矩阵：独立任务用 subagent，需直接协调用 Teams

### 其他更新
- OpenViking 15.4K⭐ 媒体爆发，宣称降低 95% Agent 记忆成本
- GSD-2 增长至 1,866⭐，新增 agent-instructions.md，但出现无限循环 Bug
- 教育 AI 进入政策驱动期（两会政协委员引用 + SAT 培训行业被 AI 颠覆）

## 专家数量修正
- 实际专家数：**30 个**（不含 Leader）
- 旧数字 27 是 PM 合并前的记录
- 已修正：产品定义文档 + PROJECT.md 统一为 30
- MEMORY.md 中的 "27 位专家" 描述需要在下次编辑时更新

## 系统配置变更（17:37 - 18:26）

### 心跳间隔调整
- **老板偏好**：30 分钟（不是 5 分钟）
- **已修改**：`openclaw.json` 中的 heartbeat 配置
- **主会话模型**：`aiae/qwen3.5-plus-2026-02-15`（成本优先）

### 模型分层策略（宪法级）
- **主会话**：`aiae/qwen3.5-plus-2026-02-15` — 90%+ 成本节省，适合规划/沟通/审查
- **ACP 开发**：`ndhy-gateway/claude-sonnet-4-6` — 编码 SOTA，acpx 只支持 Anthropic 协议
- **ACP 不能用 Qwen**：acpx 后端 v0.3.0 仅支持 Anthropic 协议，Qwen 是 OpenAI 格式

### ACP git commit 规则（宪法级）
- **来源**：V6 回滚教训 + 老板 17:37 要求
- **规则**：每轮 ACP 修改完成后必须先 git commit 才能开始下一轮
- **原因**：没有 commit = 没有安全回滚点 = 无法隔离问题
- **已写入**：AGENTS.md 宪法 + MEMORY.md

## VS Code Claude Code 故障修复（17:37 - 17:45）

### 问题
- 老板报告："我的 claude code 怎么用不了了"
- VS Code Claude Code 插件无法连接

### 根因
- `ndhy-proxy.js` 进程挂了 → 18795 端口无服务
- VS Code 配置 `ANTHROPIC_BASE_URL: http://127.0.0.1:18795` 无法连接
- 18795 是模型网关端口（ndhy-proxy），负责 Anthropic 格式 ↔ OpenAI 格式转换

### 修复
- 手动重启 ndhy-proxy 进程
- 18795 端口恢复监听
- API 验证通过

### VS Code 模型配置
- `claudeCode.selectedModel`: `opus[1m]`（Claude Opus 4.6）
- **实际使用**：`qwen3.5-plus-2026-02-15`（ndhy-proxy 自动映射）
- 映射逻辑：`claude-opus-4-6` → `qwen3.5-plus-2026-02-15`

## 外网访问修复（18:21 - 18:26）

### 问题
- 老板问："你现在可以访问外网吗？"
- web_search/web_fetch 工具返回 "fetch failed"

### 根因
- `https_proxy` 环境变量被设为空字符串（`' '`）
- curl 报错：`Unsupported proxy syntax in ' ': Malformed input to a URL function`
- 所有经过 Brave API 的请求都失败

### 修复步骤
1. 清除用户级环境变量：`http_proxy` 和 `https_proxy`
2. 清除当前会话代理变量
3. 重启 Gateway（`openclaw gateway restart`）

### 验证
- ✅ curl 直接访问外网正常（example.com）
- ✅ Brave API 调用正常（PowerShell Invoke-WebRequest 返回搜索结果）
- ✅ web_search 工具恢复正常

### 教训
- 空字符串代理配置会破坏所有 HTTP 请求
- Gateway 重启才能继承新的环境变量
- 排查顺序：系统外网 → 代理配置 → API 调用 → 工具层
