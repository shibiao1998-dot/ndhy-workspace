# 2026-03-17 工作日志

## 日终收盘（来自 2026-03-16）
- git 备份：commit 74445d29 + 44327971，push 成功
- MEMORY.md 新增规则已落地 AGENTS.md（指令包第 6 层、子 Agent 巡查、ACP 监控铁律）
- 待办遗留：`prompt-evaluation` Skill 待创建；V4 官网第二轮审查待安排；`.gitignore` 缺 `node_modules/`

## 组织重大进化（宪法级变更，老板批准）

### ACP-First 架构
- 本质：Agent Loop（N 次短调用）+ 自动压缩 + 文件即记忆，vs subagent 的 1 次调用
- 决策树：≤15KB → subagent，>15KB 或多轮迭代 → ACP，不确定 → 默认 ACP
- 三条铁律：①每步增量写入 ②CLAUDE.md 必须部署 ③标准开头
- 指令包从 6 层升级为 7 层（新增"执行模式指令"）

### Agent Team 强制使用
- 所有 ACP 开发任务必须包含 Agent Team 指引（spawn teammates 并行）
- 适用：多文件修改、大型重构。不适用：单文件、简单配置

### MCP 全量配置（最终状态 6/7 通过）
| MCP | 状态 |
|-----|------|
| ddgs-search | ✅ |
| fetch | ✅ |
| chrome-devtools | ✅（修复浏览器冲突后通过） |
| context7 | ✅（包名修复：→@upstash/context7-mcp@latest） |
| github | ✅ |
| playwright | ✅（包名修复：→@anthropic/mcp-server-playwright）待首次加载 |
| sequential-thinking | ⚠️ 配置正确但待首次加载验证 |

### 心跳机制修复
- 频率 30min → 5min，模型 opus → sonnet-4-6
- lightContext 先设 true（导致心跳失效——Sonnet 无法执行实际巡查）→ 改回 false
- 教训：lightContext=true 让心跳只看 HEARTBEAT.md，无法巡查实际目录

### 每日专家职级评估 Cron
- Job ID: 02b4cce8-1f49-440e-acdf-cb2126e020bf
- 每天凌晨 1:00 触发，评估后报告（不自动晋升）

## V5 官网进度
- Phase 1（设计补丁）✅：3 个 subagent 并行完成（动效/视觉/叙事）
- Phase 2.1（P0 阻断 + 反馈修复）✅：ACP 执行，11 文件修改，build 成功
- Phase 2.2（P2 建议 + 等级徽章）⏳
- Phase 3（审查+测试+部署）⏳

## 教训
- **监控失职**：V5 Phase 2.1 ACP 完成到确认延迟 31 分钟（根因：lightContext=true）
- **跨会话 HEARTBEAT 孤立记录**：04:03 巡查发现 3 条孤立任务记录（前会话残留）。跨会话恢复时必须先验证 subagents list 再信任 HEARTBEAT.md
- **MCP 包名踩坑**：context7/playwright/sequential-thinking 首次配置包名均有误，需要实测验证

## 调研发现归档（从 MEMORY.md HOT 层降级，2026-03-18 压缩）
> 以下条目源自 2026-03-17 定期调研，从 MEMORY.md 降级到 WARM 层

- **OpenPencil（724⭐）**：首个支持 Agent Teams 的开源 AI 矢量设计工具。Design-as-Code + MCP + Figma 导入
- **LobeHub（73.7K⭐）Agent Groups**：Agent 协作空间 + Harness 级编排
- **Agent-Teams-Lite（785⭐）**：零依赖纯 Markdown Spec-Driven Development，orchestrator + 9 专业子Agent
- **Antfarm（2.1K⭐）**：基于 OpenClaw 的一键 Agent Team 构建工具
- **教育AI"Agency"方向**：Washington Post+Guardian 报道反思——培养学生对AI的主动权
- **Opus 4.6 Context Compaction**：InfoQ 报道自适应推理和上下文压缩，专为长时间Agent设计
- **Context Design 正式命名**：SDG Group 定义 2026 年关键趋势，从单条指令到动态信息生态
- **Agent 记忆从"检索"走向"学习"**：cognee(14K⭐)=向量+图+认知科学；hindsight(4.3K⭐)=LongMemEval SOTA
- **AGENTS.md 分层策略**：OpenAI 内部 88 个 AGENTS.md，根文件全局默认，子目录本地覆盖
- **Microsoft Agent Framework 统一**：AutoGen + Semantic Kernel + TaskWeaver 合并
- **教育AI蓝海**：GitHub高质量教育AI开源项目几乎不存在。Ohio State要求所有新生学AI
- **Google Stitch AI**：收购 Galileo AI。文本→UI生成→导出Figma/React。免费350次/月
- **Forge Orchestrator（34⭐）**：接口契约优先 + 并行Agent + git worktree + 多门审查流水线
- **oh-my-claudecode（10K⭐）**：Claude Code 多 Agent 编排框架，5种执行模式+智能模型路由
- **ClawPort（550⭐）**：OpenClaw 生态可视化管理中心，org chart + kanban + 成本追踪
- **LangChain Deep Agents**：multi-step Agent 运行时，write_todos + 文件系统上下文 + subagent 隔离
- **MetaGPT 可执行反馈机制**：子 Agent 产出代码后自动跑 test/lint 反馈迭代

## 每日收盘（00:03，心跳 session 执行）
- **备份**：✅ git commit bba3b96b（150 files changed），❌ push 失败（GitHub 网络 connection reset）
- **记忆变更**：
  - 无降级需要（所有条目 < 30 天）
  - 无晋升需要（主会话已在 21:26 压缩 MEMORY.md，从 34KB→13.7KB）
  - 2026-03-17.md 已被主会话压缩（6.6KB→2.5KB→当前版本含降级条目）
- **能力缺口**：
  - `prompt-evaluation` Skill 仍待创建（连续 2 天）
  - Claude Code HUD 未安装（P0 行动项，截止 3/21）
  - PE-v2 缺 Design Token 体系（17 处裸色值）
- **代码扫描**：23:00 首次执行，2 个项目 build 通过，零乱码零阻断
- **日报**：23:36 产出 daily-report-2026-03-17.md，无法从心跳 session 发飞书
- **复盘**：01:00 / 09:00 / 17:00 三次 8 小时复盘报告已产出
- **配置调整**：无
- **待处理**：git push 需下次网络恢复时重试
